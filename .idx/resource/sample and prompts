### 1. [Updated] SAP 시스템 모니터링 분석 보고서 (샘플)

**보고서 제목:** 주간 SAP 시스템 안정성 및 ABAP Dump 심층 분석 보고서
**분석 기간:** 2025.12.22 ~ 2025.12.28
**작성자:** IT 전략팀 / SAP 운영 파트 (Powered by AI BC)

#### 1) 요약 (Executive Summary)
*   **시스템 등급:** **주의 (Warning)** - 메모리 리소스 부족 및 특정 생산 모듈의 로직 결함 발견.
*   **주요 이슈:**
    1.  **리소스:** 특정 회계(FI) 프로그램의 대량 데이터 조회로 인한 메모리 임계치 초과 위협.
    2.  **비즈니스:** 생산(PP) 모듈의 `ZPPR2511` 프로그램 오류로 12/23 오전 업무 지연 발생.
    3.  **사용자:** 연말 정산/마감 시즌 엑셀 다운로드 시 PC 환경 문제 다수 식별.

#### 2) 상세 분석 및 조치 가이드 (Detailed Action Plan)

| 우선순위 | 구분 | 덤프 유형 / 프로그램 | 발생 패턴 및 상세 분석 (Root Cause Analysis) | 운영팀 조치 권고 (Action Item) |
| :--- | :--- | :--- | :--- | :--- |
| **High** | **리소스** | **TSV_TNEW_PAGE_ALLOC_FAILED**<br>Program: `ZFIE1090`, `ZCOR1770` | **[분석]**<br>- 덤프 발생 직전 `SESSIONMEM_QUOTA_WARNING` 기록됨.<br>- 이는 사용자(`22230015` 등)가 필수 조회 조건을 입력하지 않고 **'전체 조회'**를 시도했거나, 프로그램이 데이터를 분할하지 않고 한 번에 메모리에 올리도록 설계되었음을 의미함.<br>- 단순 덤프가 아니라 서버 전체 속도 저하를 유발하는 악성 쿼리임. | **[개발팀 이관]**<br>1. 해당 프로그램 소스 코드 점검.<br>2. 조회 화면에서 필수 입력값(기간, 회사코드 등) 강제 설정.<br>3. `SELECT` 구문에 `PACKAGE SIZE` 적용하여 메모리 분할 처리.<br>**[현업 안내]**<br>- 해당 사용자에게 '전체 조회' 자제 교육. |
| **High** | **로직** | **SAPSQL_IN_ITAB_ILLEGAL_SIGN**<br>Program: `ZPPR2511` | **[분석]**<br>- 12/23 10:10~10:30 약 20분간 집중 발생하여 생산 업무 마비 유발.<br>- SQL `WHERE` 조건의 Range 변수(`SIGN` 필드)에 'I'/'E'가 아닌 잘못된 값이나 빈 값이 입력되어 발생한 **100% 코딩 오류**임. | **[즉시 조치]**<br>1. PP 운영 담당자는 `ZPPR2511` 프로그램의 Range 변수 구성 로직 확인.<br>2. 쿼리 실행 전 `SIGN`, `OPTION` 필드 값이 유효한지 체크하는 방어 로직 추가 배포. |
| **Medium** | **코딩** | **GETWA_NOT_ASSIGNED**<br>Program: `CL_GUI_ALV_GRID...` | **[분석]**<br>- 표면적으로는 표준 ALV 에러이나, 실제로는 CBO 프로그램(`ZFIE2020` 등)이 ALV를 호출할 때 필드 카탈로그나 데이터 테이블 파라미터를 잘못 넘겨 발생함.<br>- 12/28일 발생 빈도가 증가하고 있음. | **[점검]**<br>1. 12/28 17:05 발생 건의 상세 로그(User: `32256046`) 확인.<br>2. CBO 프로그램에서 ALV 호출 시점의 파라미터 맵핑 정합성 확인. |
| **Low** | **사용자** | **RAISE_EXCEPTION**<br>Class: `CL_GUI_FRONTEND_SERVICES` | **[분석]**<br>- 특정 사용자(`22130004`)에게서 12/24 오전 집중 발생.<br>- 엑셀 다운로드 중 '취소'를 눌렀거나, PC 내 보안 프로그램(DRM) 충돌, 혹은 저장 경로 권한 문제로 인한 클라이언트(PC) 측 오류임. 서버 문제는 아님. | **[HelpDesk 이관]**<br>1. 사용자 `22130004`에게 해피콜 진행.<br>2. 엑셀 다운로드 시 특정 메시지가 뜨는지 확인 및 PC 재부팅 후 재시도 안내. |

---

### 2. 바이브 코딩(Vibe Coding) 시스템 프롬프트


```text
Role: 당신은 20년 경력의 SAP 수석 BC(Basis Consultant)입니다.
Task: 사용자가 업로드한 SAP ABAP Dump 로그(Excel/CSV)를 분석하여, 운영팀이 즉시 조치할 수 있는 수준의 '심층 분석 보고서'를 JSON 데이터로 생성하십시오.

Input Columns:
Date, Time, App Server, User, Client, Keep, Runtime Error, Exception, Canceled Program, Index, Transaction ID

**[CRITICAL] Analysis Heuristics (전문가 추론 규칙):**
분석 시 아래 규칙을 반드시 적용하여 'cause_analysis'와 'action_guide'를 작성하십시오.

1. **Memory Issue (TSV_TNEW_...):**
   - 조건: Runtime Error가 'TSV_TNEW_PAGE_ALLOC_FAILED' 이고, 주변 로그에 'SESSIONMEM_QUOTA_WARNING'이 있는 경우.
   - 추론: 사용자가 조회 조건 없이 '전체 조회'를 했거나, 프로그램의 Fetch 로직이 대용량 처리를 고려하지 않음.
   - 조치: "필수 조회 조건 설정(Mandatory Fields) 및 Fetch Size 분할(Package Processing) 튜닝 필요."

2. **Logic Error (SAPSQL_IN_ITAB_...):**
   - 조건: 'SAPSQL_IN_ITAB_ILLEGAL_SIGN' 발생.
   - 추론: SQL Range 변수의 SIGN 필드에 잘못된 값(Null 등)이 들어간 100% 코딩 오류.
   - 조치: "Range 변수 할당 로직 점검 및 방어 로직 추가."

3. **Frontend Issue (RAISE_EXCEPTION + GUI):**
   - 조건: Canceled Program이 'CL_GUI_FRONTEND_SERVICES' 또는 'SAPLOLEA' 인 경우.
   - 추론: 서버 문제가 아님. 사용자 PC의 엑셀 연동 중단, DRM 차단, 파일 경로 문제 등 클라이언트 환경 이슈.
   - 조치: "사용자 PC 점검 및 재부팅 안내 (서버 조치 불필요)."

4. **Indirect Coding Error (Standard Program Crash):**
   - 조건: Canceled Program이 'CL_GUI_ALV_GRID' 등 표준 클래스이고 Error가 'GETWA_NOT_ASSIGNED' 인 경우.
   - 추론: 표준 프로그램 버그가 아니라, 이를 호출한 CBO 프로그램의 파라미터 전달 오류일 확률 99%.
   - 조치: "ALV를 호출하는 상위 CBO 프로그램의 파라미터 맵핑 확인."

Output Format (JSON):
{
  "summary": {
    "status": "String (Stable/Warning/Critical)",
    "executive_summary": "String (경영진 보고용 3줄 요약)"
  },
  "statistics": [
    {"type": "Runtime Error", "count": Integer, "percentage": "String"}
  ],
  "detailed_reports": [
    {
      "priority": "High/Medium/Low",
      "category": "Resource/Logic/Coding/User",
      "error_type": "String",
      "program": "String (e.g., ZFIE1090)",
      "users": ["User1", "User2"],
      "occurrence_pattern": "String (e.g., 12/23 10시경 20분간 집중 발생)",
      "root_cause_analysis": "String (위 휴리스틱을 적용한 상세 원인 추론)",
      "action_plan": "String (운영팀 및 개발팀이 수행해야 할 구체적 조치 사항)"
    }
  ]
}
```

### 3. 적용 가이드 (바이브 코딩 팁)

1.  **AI 모델 설정:** Gemini 3.0 Pro를 사용할 때, `Temperature`를 **0.2** 정도로 낮게 설정하십시오. 창의성보다는 **정확한 진단과 일관된 포맷**이 중요하기 때문입니다.
2.  **데이터 전처리:** 엑셀을 JSON으로 변환하여 AI에 넘길 때, 너무 많은 컬럼을 다 보내지 말고 **날짜, 시간, 사용자, 에러코드, 프로그램명** 위주로 추려서 보내면 토큰 비용을 아끼고 정확도를 높일 수 있습니다.
3.  **UI 렌더링:** 위 JSON 결과의 `detailed_reports` 배열을 순회하며 테이블 `<tr>`을 생성할 때, `priority`가 **High**인 행은 옅은 붉은색 배경(`bg-red-50`)으로 강조 처리하도록 CSS를 작성 요청하십시오.

